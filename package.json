{
  "name": "gemini-md",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/electron/main.js",
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "rm -rf dist && tsc && cp renderer/*.html dist/renderer/ && tsc --module commonjs --outDir dist/preload preload/index.ts && mv dist/preload/index.js dist/preload/index.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^31.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "simple-git": "^3.28.0"
  }
}
